# Deny by default. agent-man creates a second allowlist .gitignore inside each enabled profile.
/*
!/.gitignore
!/.gitattributes
!/README.md
!/.grok/
!/.claude-code/
!/.codex/
!/.opencode/
!/.pi-agent/
!/.gemini/
!/.agents/

# Add rules below to narrow synchronization further. Built-in profile allowlists cannot be widened.
